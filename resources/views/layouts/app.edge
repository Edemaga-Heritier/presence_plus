<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@!section('title','Présence')</title>
    <link rel="stylesheet" href="{{ asset('css/app.css') }}">
</head>
<body class="bg-gray-100 text-gray-900 flex flex-col min-h-screen">
    <header class="bg-white shadow-md sticky top-0 z-50">
        <div class="max-w-7xl mx-auto flex justify-between items-center py-4 px-6">
            <a href="/" class="text-lg font-bold text-blue-600">Présence</a>
<nav class="flex items-center gap-6">
    <a href="/" class="text-gray-600 hover:text-blue-600 transition">Accueil</a>
    @if( auth.user ) 
    <a href="/dashboard" class="text-gray-600 hover:text-blue-600 transition">Dashboard</a>
    <a href="/logout" class="bg-red-500 text-white px-3 py-1.5 rounded hover:bg-red-600 transition">Deconnexion</a>
        
    @end

</nav>

            
        </div>
         
    </header>
   
        <main class="flex-1 max-w-5xl mx-auto px-6 py-10">
            @!section('content')
        </main>

        <footer class="bg-white border-t mt-10">
            <div class="max-w-7xl mx-auto px-6 py-4 text-center text-sm text-gray-500">
                &copy; {{ new Date().getFullYear() }} Présence Plus. Tous droits réservés.
            </div>
        </footer>
</body>
</html>