@extends('layouts/app')
@section('title','Mon tableau de bord')

@section('content')
<div class="bg-white shadow rounded-lg p-6 flex items-center mb-6">
    <img src="{{user.avatarUrl}}" alt="Avatar" class="w-16 h-16 rounded-full mr-4 border">
</div>
<div>
    <h1 class="text-xl font-bold">Bonjour,{{user.name}}</h1>
<p class="text-gray-500">{{user.email}}</p>
</div>
</div>

<div class="bg-white shadow rounded-lg p-6 mb-6">
    <h2 class="text-lg font-semibold mb-4">Marquer ma présences</h2>
    <form action="/presence" method="POST" class="grid grid-cols-1 md:grid-cols-3 gap-4">
    @csrf
<input type="date" name="date" id="" class="border border-gray-300 rounded px-4 py-2 w-full md:w-auto" required>
<input type="time" name="time" id="" class="border border-gray-300 rounded px-4 py-2 w-full md:w-auto" required>
<button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 transition">Marquer</button>

    </form>
</div>

<div class="bg-white shadow rounded-lg p-6">
    <h2 class="text-lg font-semibold mb-4">Historique de présence</h2>
    @if(presences.length === 0) 
       <p class="text-gray-500">Aucune présence enregistrée pour le moment</p> 
       @else
    <div class="overflow-x-auto">
        <table class="w-full table-auto">
        <thead>
            <tr>
                <th class="px-4 py-2 border-b">Date</th>
                <th class="px-4 py-2 border-b">Heure</th>
            </tr>
        </thead>
        <tbody>
            @each(presence in presences)
            <tr>
                <td class="px-4 py-2 border-b">{{ presence.date }}</td>
                <td class="px-4 py-2 border-b">{{ presence.time }}</td>
            </tr>
            @endeach
        </tbody>
        </table>
    </div>
       @endif
</div>

@endsection
